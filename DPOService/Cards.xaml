<UserControl x:Class="DPOService.Cards"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" Name="infoCard" Width="170" Height="65">
    <Border Margin="0 0 1 0" >
 
        <Grid>
                 
            <StackPanel Margin="10 10 1 1" VerticalAlignment="Bottom">
                <TextBlock Text="{Binding Path=Title,ElementName=infoCard}" Foreground="Black" FontSize="14"/>
                <TextBlock Text="{Binding Path=Number,ElementName=infoCard}" Foreground="Black" FontSize="28" FontWeight="SemiBold"/>
            </StackPanel>
        </Grid>
        
    </Border>
</UserControl>
